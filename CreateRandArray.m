function [randArray]=CreateRandArray(handles)
% This function can be called to to bring the RandArray into a function. It is used so far in the StrategyOne function. 
randArray=handles.randTable.Data;
